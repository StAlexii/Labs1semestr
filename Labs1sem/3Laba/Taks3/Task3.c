#include <stdio.h>
#include <locale.h>

int main() {
    int a, b;                                                                                                                                                              
    setlocale(LC_ALL, "RUS");
    printf("¬ведите первое число (a): ");
    scanf_s("%d", &a);
    printf("¬ведите второе число (b): ");
    scanf_s("%d", &b);
    char* c1 = "a", * c2 = "b";
    printf("---------------------------------------------------------------------------------------------------------------------------|\n");
    printf("|   %*s * %*s         |     %*s + %*s                |   %*s - %*s               |  %*s / %*s\n", 5, c1, 5, c2, 5, c1, 5, c2, 5, c1, 5, c2 ,5, c1, 5, c2);
    printf("---------------------------------------------------------------------------------------------------------------------------|\n");
    printf("| %*d * %*d         |     %*d + %*d              |  %*d - %*d              |  %*d / %*d   \n",6,a,6,b,6,a,6,b,6,a,6,b,6,a,6,b);
    printf("---------------------------------------------------------------------------------------------------------------------------|\n");
    printf("|      %*d         |       %*d                 |    %*d                 |   %*d\n", 10,a*b,10,a+b, 10, a-b, 10, a/b);
    printf("---------------------------------------------------------------------------------------------------------------------------|\n");
    return 0;
}
